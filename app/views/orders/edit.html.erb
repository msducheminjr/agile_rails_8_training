<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl"><%= t(".legend") %></h1>

  <%= render "form", order: @order, from_view: "edit" %>

  <%= link_to "Show this order", order_path(@order, locale: I18n.locale), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
  <%= link_to "Back to orders", orders_path(locale: I18n.locale), class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
